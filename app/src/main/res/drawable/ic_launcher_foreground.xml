<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- ic_launcher_background.png already contains the full artwork (mic centered in
         the colorful burst), so this layer draws nothing on top of it. A fully
         transparent full-canvas path is used instead of an empty vector, since some
         launchers handle a zero-content adaptive-icon layer inconsistently. -->
    <path
        android:pathData="M0,0h108v108h-108z"
        android:fillColor="#00000000" />
</vector>
